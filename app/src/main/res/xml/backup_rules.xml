<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude all app data from backup to protect credentials -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
